import React, { PropTypes } from 'react'
import MuiCard from 'material-ui/Card/Card'

const Card = ({ style, ...props }) =>
    <MuiCard style={{
        overflow: 'auto',
        ...style,
    }} {...props} />

Card.propTypes = {
    style: PropTypes.object,
}

export default Card
