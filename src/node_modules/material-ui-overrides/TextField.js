import React, { PropTypes } from 'react'
import MuiTextField from 'material-ui/TextField'

const TextField = ({ style, ...props }) =>
    <MuiTextField style={{ width: '100%', ...style }} {...props} />

TextField.propTypes = {
    style: PropTypes.object,
}

export default TextField
